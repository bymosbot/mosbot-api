apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mosbot

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  # - secret.yaml  # Uncomment when secret.yaml is created from template

images:
  - name: mosbot-api
    newName: ghcr.io/mosufy/mosbot-api
    newTag: "latest"  # Override in overlays with specific version
